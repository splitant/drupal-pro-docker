# Drupal pro docker


## About The Project

The goal is to set up fastly a local Drupal project with docker environment for professional uses.

## Getting Started

### Installation

   ```sh
   make create-setup <project> <repo-git>
   # Fill GITLAB_TOKEN in .env
   make copy-files
   make setup
   ```
